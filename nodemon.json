{
  "ignore": [
    "node_modules",
    "**/*.test.ts",
    "**/*.spec.ts",
    ".git"
  ],
  "watch": ["src", "types"],
  "env": {
    "NODE_ENV": "development"
  },
  "ext": "ts,js",
  "exec": "yarn tslint && ts-node --transpile-only src/index.ts"
}
